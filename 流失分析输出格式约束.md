流失分析 —— 输出格式约束（喂给大模型的规则）

目的：约束「流失分析」模块的报告输出，用一张表展示流出去向：整体流失作为汇总行 + 重点流失竞品明细。
配合《输出格式约束-通用》一起使用（进度条、行数、隐藏空列、对比维度等规则同样生效；竞品商品名称按通用规则4做PC商详链接）。
业务口径参考《流失分析（单品诊断Agent）》：流出=访问了本品但没买本品，去买了其他SPU/SKU。

一、只输出一张表（不要拆成两张）
流失分析的「流出去向」结论，用一张表呈现，不要分成"整体表"和"竞品表"两张：
1）第一行=整体流失汇总行：把本品整体的流出/净流失规模等全局信息放在这一行（首列名称写"整体/全部竞品"）。整体信息若内容较多，可只在汇总行保留最关键的规模与竞争态势（如总流出客户数、净流失），其余用一句话结论带出。
2）其后=重点流失竞品明细行：主要抢走本品客户的竞品，每个竞品一行；竞品超过5个时只取最需关注的前5个（见二）。

二、表格结构（严格遵守）
· 第1列 = 竞品的商品名称；整体汇总行首列写"整体/全部竞品"。竞品名称是商品标题类，按通用规则4做成京东商城PC端商详链接（见四）。
· 第2列 = 该竞品诊断出的结论（一句话，10个中文字符以内，如"大单流失""抢量在加速""小幅流出"；整体行写整体竞争态势，如"净流失、竞争劣势"）。
· 其余列 = 竞品流失的关键指标（见三）。总列数4–6个（含第1列名称、第2列结论在内一起算，见通用规则2），即关键指标列为2–4个，挑与当前诊断最相关的；按通用规则3隐藏整列全空的列。
· 核心指标带环比：对最能说明问题的1–2个核心指标（如流出成交客户数、净流失成交客户数），在数值后附环比（如↑+18% / ↓-8%），让商家看出抢量在加速还是放缓；次要指标可只给当期值（通用规则8，此处对比维度指定为环比）。
· 汇总行放在第一行，之后是竞品明细，按「流出成交客户数占比」降序。竞品超过5个时，只取最重要、商家最需要关注的前5个（优先流出规模大或抢量在加速的竞品），并在表格下方标注：（除整体行外，按流出客户数占比取重点Top5，共N个流出竞品）。若竞品不足5个，有几个展示几个。
· 整体行与竞品明细行的列口径保持一致（同样的指标列），汇总行填整体数值，竞品行填该竞品数值。
· 占比类指标列，整体汇总行（第一行）该列应为100%：竞品占比是相对整体的分解，各竞品之和=整体，故整体行显示100%（用进度条时为满格100%），不要留空或写"—"。

三、可选指标（列）——由模型按实际情况自选
本模块数据表的可选原子指标共6个（每个指标都带属性：时间=近7天/昨天；对比=环比&同比；聚合=SPU/SKU；趋势=近7天By天）：
1）流出成交客户数
2）流出成交商品件数
3）流入成交客户数
4）流入成交商品件数
5）净流失成交客户数（=流出客户数-流入客户数）
6）净流失成交商品件数（=流出件数-流入件数）
选择原则：不要把6个指标全铺上，模型根据当前诊断结论和数据表实际有值的情况自行挑选相关列；无相关性或整列全空的指标不选（通用规则3）。通常以「流出成交客户数」为主轴对竞品排序，净流失类指标（客户数/件数）用于判断竞争态势（>0劣势、≈0正常、<0优势，需带出业务判断词），流入类指标按需展示。
派生展示列（非原子指标，可由上面指标计算得到，供展示用）：
· 流出客户数占比 = 该竞品流出客户数 / 总流出客户数，可用进度条（是否使用由模型决定，通用规则1）；整体汇总行该列为100%（各竞品占比之和=整体，故整体行显示100%，不要留空）。
· 流出件数占比 = 该竞品流出件数 / 总流出件数，可用进度条；整体汇总行该列同样为100%。
属性使用：时间/对比（环比、同比）/趋势（近7天By天）按诊断需要选取，例如需要看变化时带上环比或近7天趋势；聚合按分析对象选SPU或SKU。

四、竞品名称跳转（点击名称→京东商城PC端商详）——硬性要求，逐行都要加
竞品名称属商品标题类，每一行竞品都必须做成京东商城PC端商详链接，不是"可加可不加"（通用规则4）。
链接格式（Markdown）：[竞品SKU名称](https://item.jd.com/{竞品skuId}.html)
· {竞品skuId}取输入数据表该竞品行的真实SKU ID，逐行各取各的、不要共用一个ID。
· 不要因为"不确定要不要加"或"以为拿不到ID"就写成纯文本；只有该行数据表里确实没有skuId时才退回纯文本（极少数）。
· 点击竞品名称即在新标签页打开京东商城PC端商详页（商家在PC端使用报告）。
· 例外：整体汇总行首列写"整体/全部竞品"，它不是具体商品、不加链接，保持纯文本。

五、输出示例（选用了：流出成交客户数、流出客户数占比、净流失成交客户数；第1列名称、第2列结论≤10字、核心指标带环比）：
· 整体　结论"净流失、竞争劣势"　流出成交客户数1860（↑+12%）　占比100%　净流失+540
· 某品牌A旗舰款500ml（链接 https://item.jd.com/100012345678.html）　结论"大单加速抢量"　流出客户数708（↑+18%）　占比38%　净流失+520
· 某品牌B经典款（链接 https://item.jd.com/100087654321.html）　结论"稳定流出"　流出客户数372（→持平）　占比20%　净流失+90
· 某品牌C家庭装（链接 https://item.jd.com/100055556666.html）　结论"流出在收窄"　流出客户数260（↓-8%）　占比14%　净流失-30
· 某品牌D便携装（链接 https://item.jd.com/100033334444.html）　结论"抢量在加速"　流出客户数186（↑+22%）　占比10%　净流失+110
· 某品牌E尝鲜装（链接 https://item.jd.com/100011112222.html）　结论"小幅流出"　流出客户数149（→持平）　占比8%　净流失+20
（除整体行外，按流出客户数占比取重点Top5，共12个流出竞品）
（本示例仅演示选列方式，实际列由模型按诊断需要和数据表有值情况自选，见第三节）

六、模型自检清单（流失分析专用）
1）是否只输出了一张表（整体作为首行汇总行，其后是重点竞品明细），没有拆成两张？
2）表格是否为「第1列竞品商品名称、第2列结论（≤10字）、其余列为竞品流失关键指标」的结构（首行为整体汇总行）？
3）最核心的1–2个指标是否带了环比？竞品超过5个时是否只取了最需关注的重点Top5并标注总数？
4）除整体行外，每一行竞品名称是否都做成了京东商城PC端商详链接（有几行竞品就有几个链接，没有一行漏成纯文本；通用规则4）？
5）是否遵守通用约束（≤6行 / 隐藏空列 / 进度条按需用 / 只竞品名称加链接）？
