单品诊断报告 —— 通用输出约束（喂给大模型的规则）

适用范围：所有诊断模块（流量结构、成交、搜索、关键词、关键词来源、转化漏斗、广告、流失、评价、退款、SKU构成等）。

以下为硬性输出规则，生成报告时必须遵守。

规则1：占比类指标可用进度条展示（是否使用由模型决定，颜色统一蓝色）
"占比/比例/百分比/贡献度/集中度"类指标，可以用"进度条+数值"的方式展示，便于商家一眼看出相对大小。是否使用进度条不强制、由模型按展示效果和上下文自行决定——用进度条时按下面口径，不用时直接给数值也可以。
适用场景举例：各渠道访客数占比、各竞品流出占比、各关键词成交金额占比、集中度（Top1占比）等。
不适用场景（这些情况禁止用进度条，避免误解）：
1）绝对数值/计数：访客数、浏览量、客户数、件数、订单量等——这些是规模量，不是0–100%的占比，用进度条会让人误以为是百分比。
2）金额/客单价/UV价值：¥类指标同理，直接给数值。
3）转化率/比率类但可超过100%或无固定上限的指标：如环比增长率（可能+276%）、件数/客户数比值等——进度条无法表达，会失真。
4）趋势/变化方向：↑↓、环比±%、7天趋势等，用箭头或数值，不要进度条。
5）定性/结论类难量化内容：诊断结论、竞争态势判断（如"竞争劣势/优势"）、健康度、问题严重程度、建议等——这类没有明确0–100%数值口径的内容，不要用进度条硬凑一个百分比，直接用文字表述或分级标签（如高/中/低、优/良/差）。
原则：只有"分子/分母天然落在0–100%区间、且表达'占整体多少'的占比类指标"才可用进度条（用不用由模型决定，用则蓝色）；凡是规模量、金额、可超100%的比率、趋势变化、以及定性结论类难量化内容，一律用普通数值/箭头/文字，不要用进度条。

规则2：每个模块的行数与列数上限
控制表格规模，避免又宽又长。
1）行数：每个模块（步骤/维度）下的列表或表格，明细行数最多6行。超过6行时，按重要度（如占比、变化量、成交贡献）降序取Top6，其余合并为一行"其他"或直接省略，并在表格下方注明：（仅展示Top6，共N项）。
2）列数：每个列表/表格的总列数最多4–6个（含第1列名称/主体列、第2列结论/问题描述等所有列一起算，不是在名称列之外再加4–6个指标列）。举例：若第1列是名称、第2列是结论，则留给关键指标的就是2–4列。可选指标很多时，只挑与当前诊断结论最相关的放进这4–6列，不要把所有可选指标全铺上；其余指标按需下钻或省略。总列数不足4个时不硬凑，实际有几列有意义就展示几列。
3）不允许为了凑满行/列而输出无意义/无数据的行或列；实际有意义的不足下限时，有几个展示几个。

规则3：隐藏全空列（只展示有数据的列）
列表/表格的列指标，只展示至少有一行有数据的列。
1）若某一列在当前所有行里每一行都没有数据（空值/null/0且业务上等同无数据/"-"），则整列不展示，不要输出这个表头。
2）判断粒度：以"当前这张表实际参与展示的行"为准，逐列检查。
3）目的：避免出现一整列全是空白或"-"的无效列，让报告更干净。
4）注意：不要因为个别行缺失就删列；只有整列全空才删列。个别行缺失用"-"占位即可。

规则4：仅"商品标题"名称支持点击跳转京东商城（PC端）
商家在PC端使用诊断报告，只有商品标题类名称（本品/竞品的商品标题、SKU/SPU商品名称）才做成可点击链接，点击后在新标签页打开京东商城PC端对应商品详情页。
不做链接的名称：场域名称（一/二/三级场域）、关键词、地域名称、渠道名称、人群/标签名称等非商品标题的名称一律用纯文本，不要加链接（它们没有对应的商详页，加链接会误导）。
链接格式（Markdown）：[商品标题](https://item.jd.com/{skuId}.html)
规则细节：
1）{skuId}用该商品/竞品的真实SKU ID填充。
2）目标地址固定为PC商详页 https://item.jd.com/{skuId}.html。
3）展示文案用商品标题（可截断到合适长度并加…），href用上面的PC商详链接。
4）建议在新标签页打开（若渲染端支持，加target=_blank），避免商家离开诊断报告页。
5）只有商品标题名称列做链接，其余数值列、以及场域/关键词/地域等非商品名称列保持纯文本。
说明：https://item.jd.com/{skuId}.html 是京东商城PC端标准商详地址。若报告渲染在京麦/商智PC工作台内、需要带端内参数或埋点，按端内要求在该URL上追加参数，跳转目标（PC商详+skuId）保持不变。

规则5：报告模块数量上限
控制整份报告的模块数量，避免堆砌。
1）没有数据的模块不展示：某个分析模块（如流失分析、广告分析、评价分析等）在当前商品上没有相关数据或无诊断价值时，整个模块不输出，不要为了完整性硬凑一个空模块。
2）整份报告最少展示3个模块，最多展示6个模块：按诊断价值/问题严重度排序，优先展示最相关的模块，超出的省略或合并。
3）不允许输出无数据、无结论的占位模块。

规则6：报告文字详略与字数控制
在满足上面行数/列数/模块数限制的前提下，文字要写得充分但不冗长，并按下面字数区间控制，避免模型随意发挥、篇幅忽长忽短（字数均按中文字符计，含标点）。
1）每个模块的分析结论：说清"核心结论+关键原因"，一般3–6句、约100–180字；问题复杂、诊断价值高的模块可放宽到200字，简单模块不少于80字，避免只有一句话、看不出所以然。
2）运营动作建议卡片（如某模块需要给运营动作建议时）：每条建议约20–40字，一般给1–3条，聚焦"做什么+预期效果"，不写长篇。
3）表格是"骨架"、文字是"血肉"：数据用表格精简呈现（受行数/列数限制），解读和建议用文字补足，让商家读得懂、能落地。
4）避免空话套话：文字要围绕数据讲出信息量，不要用堆砌形容词来凑字数，也不要为了压字数而省略关键原因。
5）整份报告正文总字数：按模块估算，一般控制在1500字以内（约每模块200–250字 × 最多6个模块）；模块少时相应更短，不要为凑字数注水。

规则7：（已废弃，编号保留）
本规则原为"特定一级标题下输出运营动作建议卡片"，现已删除。为避免其余规则及各模块文档中"规则8/规则9"等引用错位，保留本编号占位，不再启用。

规则8：关键指标要带一个对比维度（环比/同比/行业对比，按需三选一）
输出表格/列表中的关键指标（如访客数、成交金额、转化率、流出成交客户数等核心指标），除了当期数值，还要展示一个对比维度，让商家判断"这个数是好是坏、在变好还是变差、在同行里处于什么水平"。
1）对比维度三选一：环比（与上一周期比，看近期变化趋势）、同比（与去年同期比，排除季节性看真实增长）、行业对比（与同行业/同类目均值比，看自身水平高低）。
2）根据实际情况选择必要的展示：不是每个对比都要上，模型根据当前诊断结论和数据表实际有值的情况，挑最能说明问题的那一个（必要时可选两个，但不要三个全铺）。例如判断近期波动用环比、判断是否跑赢大盘用行业对比、排除季节因素用同比。
3）只对关键指标做对比：次要指标可只给数值，不必每列都带对比，避免把表撑得又宽又杂（配合规则2列数上限）。
4）展示形式：用箭头+百分比或差值表示（如↑+12%、低于行业均值8%），不要用进度条（对比是可超100%的比率/变化量，见规则1）。

规则9：SPU下钻SKU时，SKU名称只展示规格/属性后缀
当被诊断商品是SPU（同一款商品下含多个SKU规格，如不同尺码/片数/颜色/套餐）时，下钻到SKU构成分析，逐个SKU展示。同一SPU下各SKU的商品标题前半段（品牌+品类+营销卖点）是重复的，逐行铺全标题会冗余、看不清差异，因此SKU名称只保留能区分各SKU的"规格/属性"后缀。
1）只展示完整标题里最后的规格、属性、套餐等差异化描述作为SKU名称，即去掉与同SPU其他SKU共有的前缀（品牌、品类、堆砌卖点词）后剩下的尾部规格描述。
2）示例：
· "帮宝适【类胎脂】新一级帮纸尿裤夏季超薄新生儿婴儿尿不湿尿裤 【超值】S76片(4-8kg) 常规款" → 只展示"【超值】S76片(4-8kg) 常规款"。
· "大卫平板拖把家用一拖净干湿两用拖布拖地神器懒人免手洗2026新款 新增5层刮污款+共2个拖布" → 只展示"新增5层刮污款+共2个拖布"。
3）若去掉前缀后名称仍很长，可再截断加…，但必须保留用于区分SKU的关键规格数字（如S76片、4-8kg），不要把规格截掉。
4）SKU名称仍属商品标题类，用规格后缀作链接文案，href用该SKU真实skuId的PC商详页（承接规则4）：[规格后缀](https://item.jd.com/{skuId}.html)。
5）被诊断对象本身就是单个SKU时，无需下钻，不做无谓拆解。

汇总：模型生成报告时的自检清单
1）占比指标若用了进度条，是否为蓝色、且仅用在0–100%的占比类指标上（是否使用由模型决定）？
2）每个模块行数是否≤6、总列数是否≤4–6（含名称列、结论列在内一起算）？超出是否已取Top并标注总数？列不足时是否有几列展示几列、没硬凑？
3）是否已隐藏所有"整列全空"的列？
4）商品标题名称是否已做成可跳转京东商城PC端商详的链接？场域/关键词/地域等非商品名称是否保持纯文本未加链接？
5）是否已隐藏无数据模块、整份报告模块数≤6？
6）每个模块结论是否说清了结论与原因、字数约100–180字（复杂≤200、简单≥80）？运营动作建议每条约20–40字？整份报告≤约1500字？
7）（已废弃，编号保留，对应已删除的规则7）
8）关键指标是否带了一个对比维度（环比/同比/行业对比，三选一，按需展示）？对比是否用箭头/数值而非进度条？
9）诊断对象是SPU下钻SKU时，SKU名称是否只展示了去掉公共前缀后的规格/属性后缀、且保留了关键规格数字？
