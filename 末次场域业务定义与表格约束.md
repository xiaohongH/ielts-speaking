末次场域业务定义 & 场域诊断表格约束（喂给大模型的知识）

目的：解决大模型对「末次场域」一级/二级/三级识别不准的问题，并约束场域诊断结果表格的输出格式。
配合《输出格式约束-通用》一起使用（进度条、行数≤6、隐藏空列等规则同样生效；注意通用规则4只对商品标题加链接，本模块的场域名称是纯文本、不加链接）。

**一、末次场域是什么**
末次场域：用户在最后一次进入本商品商详页之前所处的流量场域（即"用户是从哪个场域点进来的"）。它是流量来源的口径，用于定位流量结构和来源质量。
场域分三个层级，从粗到细逐级下钻：一级场域 → 二级场域 → 三级场域。
1）一级场域：最粗粒度的流量大类（回答"大方向从哪来"）。
2）二级场域：一级下的细分场景（回答"具体哪个场景"）。诊断展示优先用二级场域——粒度适中，既能定位问题又不过于零碎。
3）三级场域：二级下的最细来源（回答"最细的入口/位置"）。诊断表格不展示三级，仅当某个三级入口确有问题/机会时，在汇总结论文字里描述。
识别要点：三个层级是包含关系（一级 ⊃ 二级 ⊃ 三级），不是并列关系。同一条流量必然同时归属于某个一级、某个二级、某个三级。不要把不同层级的场域混在同一张表里对比。

**二、场域层级字典（本字典整理自京东商智「流量」→「流量结构分析」页的末次场域枚举，供人工参考；模型运行时以输入数据表的场域层级字段为准）**
模型必须严格按此层级归属识别，不得自行发明或跨层归类。
层级结构：
· 一级场域：站内场域（用户来自京东站内。若存在站外场域，同理作为一级并列，本字典以站内为准）。
· 二级场域（站内场域下的主要类别，以下为核心示例，非全集）：搜索、推荐、频道、我的京东、购物车、首页、商品、其他、游戏、活动、店铺、内容 等；实际以输入数据表中的场域层级字段为准，可能还有未列出的二级场域。
· 三级场域：各二级下的具体入口。
二级场域含义 & 三级明细（以下为核心示例，非全集）：
1）搜索：用户主动检索关键词后进入商详，购买意图最明确。三级入口：搜索场景入口（按搜索词）。
2）推荐：系统算法分发（猜你喜欢类），用户被动触达。三级入口：核心推荐、其他推荐。
3）频道：各类频道页/主题营销入口，受活动与频道运营节奏影响大。三级入口：京东热卖、商品榜、东东农场、特价频道、京东直播、京东超市、我的京豆、品质外卖、新品-推荐页、JOY游记、京东秒杀-推荐页、京东小游戏、便宜包邮、国补/百补-推荐页、PLUS会员频道、超级明星、国补、试用领取、京东京造、中外名酒、手机数码、拍拍二手、京东旅行、自营秒送 等。
4）我的京东：个人中心入口，站内二次触达。三级入口：我的京东。
5）购物车：购物车/再次购买，已有购买意向或复购路径。三级入口：购物车。
6）首页：APP首页各坑位分发。三级入口：首页-其他、首页-通栏一、首页-吸顶通栏。
7）商品：商详页之间跳转/商品关联。三级入口：商品。
8）其他：零散站内入口。三级入口：消息中心、小首页。
9）游戏：互动游戏引流。三级入口：种豆得豆 等。
10）活动：活动页/会场。三级入口：活动。
11）店铺：店铺首页/店铺页自有流量。三级入口：店铺。
12）内容：内容种草场域。三级入口：直播间、短视频、图文素材。
说明：上表二级场域及其三级入口均为核心示例、非全集，会随业务运营变化。模型遇到未列出的场域时，一律以输入数据表中每行携带的场域层级字段为准来判断它是一级/二级/三级，不要强行塞进上表已列出的类别，也不要凭名称猜测或新造层级；仅当数据表未提供层级、需要靠名称推断时，才参考上表；遇到三级「频道」下未列出的入口，默认归到二级「频道」。业务含义可结合内部定义继续细化。

三、场域诊断结果表格约束
3.1 表格结构（转置：指标做表头，场域做行）
· 表头（列）= 指标名称。可用指标（按组）：引入商详访客数、引入商详浏览量；流量质量：UV价值、客单价；引导转化：引入成交客户数、引入成交金额、引入成交商品件数、引入成交单量、访客-成交转化率、浏览-成交转化率。实际展示哪些列，按通用规则3（隐藏全空列）取当前有数据的列。
· 行名称（首列）= 场域名称。
· 展示层级 = 只展示二级场域。表格不展开三级，模型无需判断是否下钻三级；若某个三级入口确有问题/机会，在汇总结论文字里描述即可（如"推荐场下的'猜你喜欢'三级入口转化明显偏低"），不要在表格中单列三级行。一级场域可作为分组或汇总行，不必单独铺开整张表。
3.2 必须包含的场域范围
· 展示当前商品实际有流量的二级场域，按「访客数占比」或「访客数变化量」降序排列。
· 最多6行（通用规则2），超出取Top6并标注：（仅展示Top6，共N个场域）。
· 整列全空的指标列不展示（通用规则3）。
· 占比列可用进度条展示，是否使用由模型决定（通用规则1）。
3.3 输出示例（二级场域，Top展示）
表头示例（列）：二级场域、引入商详访客数、引入商详浏览量、UV价值、客单价、引入成交客户数、引入成交金额、引入成交商品件数、引入成交单量、访客-成交转化率、浏览-成交转化率；行示例：搜索、推荐、频道、购物车 等，按引入商详访客数（或访客数占比）降序。场域名称是纯文本，不做链接（通用规则4：只有商品标题才加京东商城PC端商详链接，场域/关键词/地域等名称不加）；若某指标列当前所有场域都无数据，则整列不展示（通用规则3）。

四、模型自检清单（场域诊断专用）
1）是否分清了一级/二级/三级的包含关系，没有跨层级混排？
2）表格是否为「表头=指标、行=场域名」的结构？
3）表格是否只展示了二级场域（三级问题放到汇总结论文字里，不在表格单列）？
4）是否遵守了通用约束（进度条按需用 / ≤6行 / 隐藏空列 / 场域名称不加链接）？
